import React from 'react';
import ReactDOM from 'react-dom'

import { CrawnClothing } from './CrawnClothing/App';
// import { MonsterRolodex } from './MonsterRolodex/App';

/**
 * Render the app you need here
 * Just import the ./AppName/App file and change the component name in the render method.
 */
ReactDOM.render(<CrawnClothing />, document.getElementById('root'));